"""Deprecated: AlgorithmServiceFactory is no longer used in the new architecture.

This module remains as a stub to avoid import errors when referenced accidentally.
"""

class AlgorithmServiceFactory:
    def __init__(self, *args, **kwargs):
        raise RuntimeError("AlgorithmServiceFactory is deprecated; use the runner in central/app/main.py")
