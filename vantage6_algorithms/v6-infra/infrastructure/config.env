PYTHON_INTERPRETER="${PYTHON_INTERPRETER:-python3.12}"
VERSION_VANTAGE6="${VERSION_VANTAGE6:-4.13.3}"
ENVIRONMENT="${ENVIRONMENT:-DEV}"
# Imputation demo data lives in ./data/imputation (filled with missing values for the central site).
ALGO_DATA_DIRECTORY="${ALGO_DATA_DIRECTORY:-./data/imputation}"
VENV_PATH="${VENV_PATH:-./venv}"
SERVER_CONFIG="demoserver.yaml"
ENTITIES_FILE="entities.yaml"
DOCKER_REGISTRY="harbor2.vantage6.ai/infrastructure"
UI_PORT="80"
UI_URL="http://localhost"
SERVER_URL="http://localhost:5070"
API_PATH="/api"
